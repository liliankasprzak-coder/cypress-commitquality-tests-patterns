name: CI - UI tests (patterns)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ui-patterns-${{ github.ref }}
  cancel-in-progress: true

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install
        run: npm ci

      - name: Run Cypress (UI patterns) - Smoke Test
        uses: cypress-io/github-action@v6
        with:
          spec: cypress/e2e/exercicio2_commitquality_tests-patterns/1-tela-login.cy.js
          browser: chrome

      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-artifacts-ui-patterns
          path: |
            cypress/screenshots
            cypress/videos
          if-no-files-found: ignore
